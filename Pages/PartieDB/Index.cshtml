@page
@model Memory.Pages.PartieDB.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Partie[0].NumberCards)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Partie[0].StateGame)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Partie[0].TournToPlay)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Partie[0].CreateAt)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Partie) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumberCards)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StateGame)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TournToPlay)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreateAt)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
